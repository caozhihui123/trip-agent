# 智能旅行规划助手 — 项目介绍

## 一、项目概述

智能旅行规划助手是一个基于多 Agent 架构的全栈 Web 应用。用户输入目的地、旅行天数、预算、偏好后，系统通过编排 5 个专业化 AI Agent 协作完成从数据采集到行程编排的全流程，生成包含景点、餐饮、酒店、交通路线和天气预报的完整旅行计划，并支持交互式编辑、地图可视化和 PDF 导出。

## 二、技术架构

### 后端（~900 行 Python）

```
backend/app/
├── agents/                          # 多 Agent 工作流
│   ├── state.py                     # TypedDict 共享状态定义
│   ├── prompts.py                   # 各 Agent 系统提示词
│   ├── graph_builder.py             # StateGraph 构建（7 节点连线）
│   └── nodes/
│       ├── init_node.py             # 输入校验 + 任务初始化
│       ├── poi_search_node.py       # POI 搜索 Agent (amap_text_search)
│       ├── weather_node.py          # 天气查询 Agent (amap_weather)
│       ├── route_planning_node.py   # 路线规划 Agent (amap_route)
│       ├── compile_node.py          # 行程编译器（LLM 生成 JSON）
│       └── budget_node.py           # 预算重算（修正 AI 误差）
├── mcp_server/
│   └── amap_mcp.py                  # FastMCP + LangChain @tool 双导出（4 工具）
├── api/
│   ├── main.py                      # FastAPI lifespan（启动时加载 MCP 工具 → 构建 Graph）
│   └── routes/
│       ├── trip.py                   # POST /api/trip/plan → graph.ainvoke()
│       └── map.py                    # 前端地图查询 REST API
├── services/
│   ├── llm_service.py               # ChatOpenAI 工厂（DeepSeek 兼容）
│   └── amap_service.py              # 高德 REST API 异步查询
└── models/
    └── schemas.py                   # Pydantic 数据模型（TripRequest, TripPlan 等）
```

**工作流：**

```
START → init ──→ poi_search ──→ route_planning → compile → budget → END
              └→ weather ────→
```

1. **init_node**：纯 Python，校验输入参数，生成初始任务描述
2. **poi_search_node**：Agent 绑定 `amap_text_search` 工具，多关键词搜索景点、餐厅、酒店
3. **weather_node**：Agent 绑定 `amap_weather` 工具，查询天气预报
4. **route_planning_node**：Agent 绑定 `amap_route` 工具，基于已发现的 POI 位置规划路线
5. **compile_node**：LLM 直接调用（无工具），注入所有前置数据，生成最终行程 JSON
6. **budget_node**：纯 Python，重新计算每个预算项（门票、酒店、餐饮、交通），修正 LLM 运算误差

**核心技术点：**
- 每个 Agent 节点使用 `model.bind_tools()` + 手动工具调用循环，而非已废弃的 `AgentExecutor`
- MCP 工具采用双导出模式：同时注册为 `FastMCP` 工具（用于协议兼容/外部发现）和 `@tool` 装饰的 LangChain `BaseTool`（运行时零序列化开销直接使用）
- 共享状态 `TripPlannerState` 使用 `TypedDict` + `Annotated[list, add_messages]` 实现跨节点消息自动累加

### 前端（~860 行 TypeScript/Vue）

```
frontend/src/
├── components/
│   ├── TripForm.vue          # 旅行参数表单
│   ├── TripResult.vue        # 行程结果展示
│   ├── DayCard.vue           # 单日行程卡片
│   ├── BudgetPanel.vue       # 预算明细面板
│   ├── MapPanel.vue          # 高德地图可视化
│   └── ExportToolbar.vue     # PDF 导出
├── services/api.ts           # Axios API 调用封装
├── stores/trip.ts            # 状态管理
└── types/index.ts            # TypeScript 类型定义
```

**前端技术：** Vue 3 (Composition API) + TypeScript + Ant Design Vue + 高德地图 JS API + jsPDF/html2canvas

## 三、关键设计决策

| 决策 | 方案 | 理由 |
|------|------|------|
| Agent 框架 | LangGraph StateGraph（非 hello-agents） | 支持有向无环图工作流、原生状态管理、节点解耦 |
| 工具调用 | `model.bind_tools()` 手动循环 | LangChain 1.x 已移除 `create_tool_calling_agent`，手动循环更可控 |
| MCP 集成 | 双导出（FastMCP + @tool） | 保留 MCP 协议兼容性，运行时零开销直接绑定 |
| LLM 提供方 | DeepSeek (OpenAI 兼容) | 成本低、中文能力强、兼容 `ChatOpenAI` SDK |
| 风扇出模式 | `add_edge` 依赖图 | 节点间并行受限于 Python GIL，串行执行不会显著影响总延迟（瓶颈在 LLM） |
| 预算处理 | 独立 `budget_node` 重算 | LLM 算术不可靠，Python 精确重算保证一致性 |

## 四、API 接口

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/trip/plan` | 提交旅行需求，返回完整行程 JSON |
| PUT | `/api/trip/plan/{id}` | 编辑已生成的行程（预留） |
| GET | `/api/map/poi/search` | 前端 POI 搜索 |
| GET | `/api/map/weather` | 前端天气查询 |
| POST | `/api/map/route` | 前端路线规划 |
| GET | `/api/map/geocode` | 前端地址转坐标 |
| GET | `/api/map/distance` | 前端距离计算 |

## 五、运行方式

```bash
# 环境：Anaconda3 py3.10
conda activate py3.10
cd backend && pip install -r requirements.txt

# 配置 .env（LLM_API_KEY + AMAP_MAPS_API_KEY）

# 启动后端（端口 8000）
uvicorn app.api.main:app --reload

# 启动前端（端口 5173）
cd frontend && npm install && npm run dev
```
